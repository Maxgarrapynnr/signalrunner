Django>=5.0,<6.0
django-q2
gunicorn
whitenoise
cryptography
requests

# Market data + indicators
casabourse
yfinance        # fallback provider when casabourse is blocked (VPS/server IPs)
pandas

# Telegram delivery uses requests (already listed above)
